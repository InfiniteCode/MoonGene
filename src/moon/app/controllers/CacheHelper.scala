//Copyright 2014, Alex Khilko.
//This file is part of MoonGene which is released under MIT.
//See file LICENSE.TXT or go to www.alexkhilko.com for full license details. 

package controllers

import play.api.cache.Cache
import play.api.Play.current

/*
  CacheHelper helps with sending and retrieving values from cache
 */
object CacheHelper {
  /*
    def getValue = Cache.getAs[ValueType]("value_name")
    def setValue(value: ValueType) { Cache.set("value_name", value, 60*60*8) /*Expiration time in seconds*/ }
   */
}
